[package]
name = "paged-llm-demo"
version = "0.1.0"
edition = "2021"
publish = false
description = "Educational demo: PagedAttention KV cache and INT8 KV quantization in a toy transformer"

[dependencies]
# Zero external deps — all randomness is from an inline xorshift LCG.
